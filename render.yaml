services:
  - type: web
    name: veilx
    env: node
    region: singapore
    plan: free
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/health